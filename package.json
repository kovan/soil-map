{
  "name": "soil-map",
  "version": "1.0.0",
  "description": "World Soil Organic Matter Map in ClojureScript",
  "scripts": {
    "dev": "shadow-cljs watch app",
    "build": "export BUILD_ID=$(git rev-parse --short HEAD) && shadow-cljs release app",
    "server": "shadow-cljs server",
    "test": "node test/hover.test.js",
    "deploy": "npx wrangler pages deploy public --project-name soil-quality-map --upload-source-maps"
  },
  "devDependencies": {
    "mapshaper": "^0.6.113",
    "shadow-cljs": "^2.28.0"
  },
  "dependencies": {
    "puppeteer": "^24.35.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
